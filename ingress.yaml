apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: demo-yu-ingress
  namespace: default
spec:
  rules:
    - host: yudk.dev.daum.net
      http:
        paths:
          - path: /
            backend:
              serviceName: demo-yu-svc
              servicePort: 80
